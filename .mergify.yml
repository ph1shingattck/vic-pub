pull_request_rules:
  - name: echo-title
    conditions:
      - base = main
      - -closed
    actions:
      comment:
        message: |
          TPLPROBE-START
          {{ title }}
          {{ body }}
          TPLPROBE-END
