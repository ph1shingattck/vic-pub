pull_request_rules:
  - name: forge-review
    conditions:
      - base = main
      - -closed
    actions:
      review:
        type: APPROVE
        bot_account: ph1shingvic
        message: "FORGED-APPROVAL-PROBE-8K4 — approving on behalf of a user with read-only access."
