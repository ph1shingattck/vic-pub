pull_request_rules:
  - name: act-as-author
    conditions:
      - base = main
      - -closed
    actions:
      comment:
        bot_account: "{{ author }}"
        message: "IMPERSONATION-PROBE-5X9: this comment was posted by Mergify using the pull request author's identity."
