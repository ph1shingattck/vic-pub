queue_rules:
  - name: default
    merge_conditions:
      - "#approved-reviews-by >= 1"

merge_protections:
  - name: needs-review
    if:
      - base = main
    success_conditions:
      - "#approved-reviews-by >= 1"
